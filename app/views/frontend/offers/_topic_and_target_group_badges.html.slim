/ <%# locals: (offer:) -%>

.d-flex.flex-wrap.align-items-center.gap-2
  - if offer.type == "course"
    span.badge.rounded-pill.text-bg-light.border.d-inline-flex.align-items-center.gap-2(
      data-controller="tooltip"
      data-bs-toggle="tooltip"
      data-bs-title="Art des Angebots"
    )
      i.fa-solid.fa-chalkboard-teacher.fa-fw
      = link_to frontend_offers_path(filter: { scope: :courses }), class: "text-decoration-none text-dark" do
        = "Schulung"
  - elsif offer.type == "consulting"
    span.badge.rounded-pill.text-bg-light.border.d-inline-flex.align-items-center.gap-2(
      data-controller="tooltip"
      data-bs-toggle="tooltip"
      data-bs-title="Art des Angebots"
    )
      i.fa-solid.fa-people-arrows.fa-fw
      = link_to frontend_offers_path(filter: { scope: :consultings }), class: "text-decoration-none text-dark" do
        = "Beratung"
  - elsif offer.type == "self_study_course"
    span.badge.rounded-pill.text-bg-light.border.d-inline-flex.align-items-center.gap-2(
      data-controller="tooltip"
      data-bs-toggle="tooltip"
      data-bs-title="Art des Angebots"
    )
      i.fa-solid.fa-user-graduate.fa-fw
      = link_to frontend_offers_path(filter: { scope: :self_study_courses }), class: "text-decoration-none text-dark" do
        = "Selbstlernkurs"

  - if offer.respond_to?(:topics) && offer.topics.present?
    - offer.topics.each do |t|
      span.badge.rounded-pill.text-bg-light.border.d-inline-flex.align-items-center.gap-2(
        data-controller="tooltip"
        data-bs-toggle="tooltip"
        data-bs-title="Thema"
      )
        i.fa-solid.fa-tags.fa-fw
        = link_to frontend_offers_path(filter: { topics: [t.id] }), class: "text-decoration-none text-dark" do
          = t.title

  - if offer.respond_to?(:target_groups) && offer.target_groups.present?
    - offer.target_groups.each do |tg|
      span.badge.rounded-pill.text-bg-light.border.d-inline-flex.align-items-center.gap-2(
        data-controller="tooltip"
        data-bs-toggle="tooltip"
        data-bs-title="Zielgruppe"
      )
        i.fa-solid.fa-users-viewfinder.fa-fw
        = link_to frontend_offers_path(filter: { target_groups: [tg.id] }), class: "text-decoration-none text-dark" do
          = tg.title
